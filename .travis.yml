language: node_js
node_js:
  - 12.18.3
cache: npm


install:
  - npm install

script:
  - npm test
  - CI=false npm run build


deploy:
  provider: pages
  skip-cleanup: true
  github-token:
  local_dir:build
  on:
    branch: master
